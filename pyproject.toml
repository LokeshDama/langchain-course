[project]
name = "agent-harnesses"
version = "0.1.0"
description = "Chapter 7 code companion — Agent Harnesses with LangChain Deep Agents"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "deepagents==0.6.8",
    "langchain>=1.0.0",
    "langchain-openai>=1.0.0",
    "langgraph>=1.0.0",
    "python-dotenv>=1.0.0",
    "langsmith",
]
